{
  "name": "lambda-orchestrator",
  "version": "1.0.0",
  "main": "src/handler.js", 
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "serverless offline --host 0.0.0.0 --noPrependStageInUrl",
    "deploy": "serverless deploy --region us-east-2"
  },
  "type": "module",
  "dependencies": {
    "axios": "^1.7.0",
    "uuid": "^11.0.0"
  },
  "devDependencies": {
    "serverless": "^3.40.0",
    "serverless-offline": "^13.0.0" 
  }
}