
# Para generar un token seguro:
#   openssl rand -hex 32
#
# El token debe tener mínimo 64 caracteres hexadecimales
SERVICE_TOKEN=CHANGE_THIS_TOKEN_GENERATE_WITH_OPENSSL_RAND_HEX_32


# Credenciales de la base de datos MySQL
MYSQL_ROOT_PASSWORD=root
MYSQL_DATABASE=orders_db
MYSQL_USER=orders_user
MYSQL_PASSWORD=root
MYSQL_PORT=3306

# --------------------------------------------------
# Customer API Configuration
# --------------------------------------------------
# Puerto expuesto para Customer API
CUSTOMERS_API_PORT=3001
# Entorno de ejecución 
CUSTOMERS_API_NODE_ENV=development

# --------------------------------------------------
# Orders API Configuration
# --------------------------------------------------
# Puerto expuesto para Orders API
ORDERS_API_PORT=3002
# Entorno de ejecución 
ORDERS_API_NODE_ENV=development

# --------------------------------------------------
# Lambda Orchestrator Configuration
# --------------------------------------------------
# Puerto expuesto para Lambda Orchestrator
LAMBDA_PORT=3000

# --------------------------------------------------
# Internal Service URLs (Docker Network)
# --------------------------------------------------
# ℹ️ Estas URLs funcionan dentro de la red Docker
# ⚠️ NO cambiar a menos que se modifiquen los nombres de servicios en docker-compose.yml
CUSTOMERS_API_BASE=http://customers-api:3001
ORDERS_API_BASE=http://orders-api:3002

# --------------------------------------------------
# Adminer Configuration (MySQL Web UI)
# --------------------------------------------------
# Puerto expuesto para Adminer
ADMINER_PORT=8080
# Servidor MySQL por defecto
ADMINER_DEFAULT_SERVER=mysql

# ==================================================
# 📝 INSTRUCCIONES DE USO
# ==================================================
# 
# PASO 1: Copiar este archivo
# ---------------------------
#   cp .env.example .env
#
# PASO 2: Generar un token seguro
# --------------------------------
#   openssl rand -hex 32
#
#   Esto generará algo como:
#   8f3e9a2b7c6d5e4f3a2b1c9d8e7f6a5b4c3d2e1f9a8b7c6d5e4f3a2b1c0d9e8f
#
# PASO 3: Editar .env
# -------------------
#   Abre .env y reemplaza:
#   - SERVICE_TOKEN con el token generado
#   - MYSQL_ROOT_PASSWORD con una contraseña segura
#   - MYSQL_PASSWORD con una contraseña segura
#
# PASO 4: Levantar el sistema
# ----------------------------
#   docker-compose up -d --build
#
# PASO 5: Verificar que funciona
# -------------------------------
#   curl http://localhost:3001/health  # Customer API
#   curl http://localhost:3002/health  # Orders API
#   curl http://localhost:3000/dev/health  # Lambda Orchestrator
#
#   Deberías ver respuestas como:
#   {"status":"ok","service":"customers-api"}
#   {"status":"ok","service":"orders-api"}
#   {"status":"healthy"}
#
